<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.acer.kuis">

    <application
        android:allowBackup="true"
        android:icon="@drawable/bagong"
        android:label="Museum Tubuh"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DeskripsiActivity" />
        <activity android:name=".FasilitasActivity" />
        <activity android:name=".WahanaActivity" />
        <activity android:name=".AnggotaActivity"/>
        <activity android:name=".HelpActivity"/>
    </application>

</manifest>